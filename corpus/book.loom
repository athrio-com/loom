# Loom

Loom is a literate programming framework. You write a document — prose and code together, in the order that reads best — and Loom tangles it, composing the code into real source files. The prose is half of the program: the half a person reads to learn what the code does and why.

This book is written in Loom, in the framework's own source, and it is the framework's only specification: the design notes that once sat apart from the code now live here, in the chapters that describe it, where they cannot drift. `loom weave` renders the book as the website you are reading — every chapter a page, its prose and its code side by side. Read it from the front. It starts with using Loom and then opens the machine, each part building on the one before.

## Table of contents

I. Using Loom
   1. A first loom
   2. Anchors and warps
   3. Many files, many languages
   4. The workspace
   5. Tangling and the sink tree
   6. The command line

II. How a loom becomes code
   1. The two planes
   2. The pipeline
   3. One model, many tiers

III. The shape of a loom
   1. The node foundation
   2. Health, built in
   3. The leaf tokens
   4. Wefts
   5. The syntax tree
   6. The corpus

IV. Reading the text
   1. Line ranges
   2. Classifying lines
   3. Tokenising
   4. Building the tree
   5. Following imports

V. The frame
   1. The generated program
   2. Sections become services
   3. The composition language
   4. The dependency graph
   5. Running the frame
   6. The product

VI. Tangling
   1. The compiler
   2. Writing files
   3. Configuration, compiled
   4. Faults
   5. The build cache

VII. In the editor
   1. The virtual tree
   2. Projecting to virtual code
   3. The plugin and the server
   4. Loom's own diagnostics

VIII. Languages as packages
   1. A service works on a product
   2. The service contract
   3. The host lends its runtime
   4. The store
   5. The TypeScript service
   6. The TypeScript baseline

IX. Loom builds Loom
   1. The self-hosting cycle
   2. The command-line program
   3. Bundling
   4. The editor extension
   5. Packaging
   6. Releasing

X. Work in progress
   1. Rename freshness
   2. Testing in looms
   3. Orphaned tangle output

## Part I — Using Loom [packages/loom-lang]

You meet Loom the way you use it: by writing a document and watching it become code. This part is the whole surface a person touches, and nothing beneath it. It begins with a single loom and grows to a real project — sections composed with anchors and warps, one narrative spread across many files and languages, a workspace Loom sets up for you, and a command line that drives the rest. By the end you can build with Loom without yet knowing how it works inside.

::[A first loom]

::[Anchors and warps]

::[Many files, many languages]

::[The workspace]

::[Tangling and the sink tree]

::[The command line]

## Part II — How a loom becomes code

Now that you have written a loom, this part says what Loom did with it. It is the short path through the idea at the centre of the framework, and the map for every part that follows. Loom keeps two planes apart: the frame, the program that assembles your code, and the product, that code itself. It carries a loom along one pipeline from text to files, and the data flowing through is a single tree, the same shape at every stage. The rest of the book walks that pipeline one pass at a time.

## Part III — The shape of a loom

A loom is text, but Loom does not work on text. It works on a tree. This part is that tree — the data model every later pass reads and enriches. Every node shares one foundation. Each carries a place in the source and its own record of what is wrong with it. On that foundation stand three layers — the smallest tokens, the lines that group them, and the sections those lines form. Above them all sits the corpus, the modules that import one another.

## Part IV — Reading the text

A loom arrives as plain text, and Loom must read it into the tree the previous part described. This part is the reading. Four passes turn text into a tree: Loom finds the line boundaries, labels each line by what it is, fills in the pieces inside each line, and folds the labelled lines into a document of sections. A fifth pass follows that document's imports out to the corpus it belongs to.

## Part V — The frame

Here the document turns into a program. Loom projects each section into a small composable unit, a service that carries the section's code, and wires the units into the frame — the program that assembles your code when it runs. The wiring comes from the document itself. A warp from one section to another is a dependency, and those dependencies fix the order. Running the frame produces the product — your code, composed at last into the files you meant to write.

## Part VI — Tangling

With your code produced, Loom writes it out. This part is the back half of the pipeline. The compiler works out where each file belongs, the tangler writes the files to disk, and the configuration — itself a loom — supplies the roots that place them. Loom refuses to write a build that still holds an unresolved reference, and when something is wrong, the author sees one plain message for it. A cache underneath keeps the next build fast.

## Part VII — In the editor

A loom is several languages at once: prose, and code in whatever languages its sections use. This part gives each of them a real editor — syntax highlighting, errors, and completion, every diagnostic mapped back to the spot you wrote. Loom projects the document into a tree of virtual files, one language to a file, and hands that tree to the editor through a single plugin and a language server. Loom's own mistakes surface here too: a malformed tag, an anchor that resolves to nothing, shown beside the code.

## Part VIII — Languages as packages

Loom composes any language, so a language's editor support cannot be built in. It arrives as a package. This part is the contract such a package satisfies and the path it travels into a project. A service works not on a file but on a product — the code one section composed. The host lends each service its runtime, so the package ships thin. `loom add` installs it into the workspace store, a registry finds it by language, and the editor loads it on demand. TypeScript is the worked example, checked against the project's own configuration.

## Part IX — Loom builds Loom

The book closes where it began, with the fact that Loom is written in Loom. This part is the cycle that keeps that honest. The published tool builds the next release, never the in-progress source. Around the cycle sit the pieces that ship it — the command-line program, the bundles that fold each package into one file, the editor extension, and the release that publishes them all. It ends on the road still ahead.

## Part X — Work in progress

This part is never finished. It holds the framework's active work — each open problem or unbuilt feature as its own loom, written to be worked out in place. Such a loom is not a specification. It opens as prose that states the problem and the shape of the answer, and it is built by amending that same loom with code, refining the prose as the design settles — never by taking it as a plan and writing the code somewhere else. So it travels from idea to implementation as one artifact, and never drifts from what it describes. When a chapter here is done, it graduates to its home part. The first three: a bug in the editor's own freshness, the move that brings the tests themselves into the book, and the orphaned files a re-tangle leaves behind.

::[Rename freshness](wip/rename-freshness.loom)

::[Testing in looms](wip/testing-in-looms.loom)

::[Orphaned tangle output](wip/orphaned-output.loom)

---