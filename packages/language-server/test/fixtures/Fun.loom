---
The integration fixture, written mainly in {{lang: Scala}}.
---

The lines above form the Document Preamble; the `lang` Warp names the
primary language. Every heading below is a flat Section — heading
level is reader-facing organisation only, the tree stays flat.

# Reading notes [Notes]

A heading at any level creates a flat Section.

=>

val readingNote = "Sections are flat on the document"

~

This prose line still belongs to the Notes Section.

# Glossary

A Section with no tag. The Tokeniser synthesises a stable hash
tag so every Section has an identifier.

# Arithmetic

A library of integer arithmetic. The primitives first, a couple of
composites on top, and a `main` that prints a few results.

## Imports

`Math.pow` is the only outside dependency.

=>

import scala.math.pow

~

Everything else is in the standard library.

## Adder [Add]

=>

def add(x: Int, y: Int): Int = x + y

## Multiplier [Mul]

=>

def mul(x: Int, y: Int): Int = x * y

## Square [Sq]

Built on top of {{m: Mul}}.

=>

{{m}}

def square(x: Int): Int = mul(x, x)

## Power [Pow]

`pow` works in `Double`; the result is rounded back to `Int`.

=>

def power(base: Int, exp: Int): Int = pow(base, exp).toInt

## Entry point [Main]

Smoke tests for {{a: Add}}, {{s: Sq}}, and {{p: Pow}}.

=>

{{a}}
{{s}}
{{p}}

object Arithmetic extends App {
  println(s"add(2, 3)    = ${add(2, 3)}")
  println(s"mul(4, 5)    = ${mul(4, 5)}")
  println(s"square(7)    = ${square(7)}")
  println(s"power(2, 10) = ${power(2, 10)}")
}

# Companion imports {Loom}

Bring `Neg` in from the companion document so a section can depend on it
across files. The import is hoisted to the head of the synthesised frame.

=>

import { Neg } from "./Sad.loom"

## Negated [Negd]

Built on `Neg` from the companion document via {{n: Neg}} — a cross-file
Warp edge. The `yield* Neg` resolves to the imported class.

=>

{{n}}

def negated(x: Int): Int = negate(x)

# Build script [Build]{Bash}

Compile the single file, then run the resulting class.

=>

#!/usr/bin/env bash
scalac src/main/scala/Arithmetic.scala -d out
scala -cp out Arithmetic

# Tangling the library {src/main/scala/Arithmetic.scala}

Pull the imports and the entry point together.

=>

{{Imports}}
{{Entry point}}

# Tangling the build script {build.sh}

=>

{{Build script}}
