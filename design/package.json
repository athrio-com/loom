{
  "name": "loom-design-catalog",
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "description": "The Loom design catalog — landing, docs, and annotation-component specimens.",
  "scripts": {
    "dev": "bunx --bun vite --port 5199",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview --port 5199"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
